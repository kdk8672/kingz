<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.review.mapper.ReviewMapper">

	<select id="reviewList" parameterType="int">
		select 	*
		from 	review
		where 	room_id = #{roomId}
	</select>
	
	<select id="insertReview" parameterType="int">
		insert into review
		(review_id, member_id, room_id, review_content, rating, review_date)
		values (seq_review_id.nextval,
		#{memberId}, #{roomId}, #{reviewContent}, #{rating}, sysdate)
	</select>

</mapper>